import React from "react";
import { Link } from "react-router-dom";

const Redirect = () => {
  return (
    <div>
      123 redirect
      <Link to="/redi">重定向测试</Link>
    </div>
  );
};

export default Redirect;
